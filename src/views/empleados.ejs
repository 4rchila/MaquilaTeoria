<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Empleados</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #eef2f7;
            font-family: 'Roboto', sans-serif;
            color: #333;
        }
        .container {
            margin-top: 50px;
        }
        .card {
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            background: #fff;
        }
        .card-header {
            background: linear-gradient(135deg, #007bff, #0056b3);
            color: #fff;
            font-size: 1.2rem;
            border-radius: 12px 12px 0 0;
            padding: 15px;
        }
        .table-container {
            margin-top: 30px;
        }
        .table thead {
            background-color: #007bff;
            color: #fff;
        }
        .table th, .table td {
            text-align: center;
            vertical-align: middle;
            padding: 12px;
        }
        .btn {
            border-radius: 8px;
            padding: 8px 12px;
            font-weight: bold;
        }
        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
        }
        .btn-success {
            background-color: #28a745;
            border-color: #28a745;
        }
        .btn-danger {
            background-color: #dc3545;
            border-color: #dc3545;
        }
        .btn:hover {
            opacity: 0.85;
        }
        h1, h3 {
            color: #0056b3;
        }
        .table tbody tr:nth-child(odd) {
            background-color: #f8f9fa;
        }
        .table tbody tr:nth-child(even) {
            background-color: #e9ecef;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="text-center">Registro de Empleados</h1>

    <div class="card mt-4">
        <div class="card-header text-center">Registrar Nuevo Empleado</div>
        <div class="card-body">
            <form action="/recursosHumanos/add" method="post">
                <h3>Datos Personales</h3>
                <div class="mb-3">
                    <label class="form-label">Nombre</label>
                    <input type="text" name="nombre" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Fecha de nacimiento</label>
                    <input type="date" name="fecha_nacimiento" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Número de teléfono</label>
                    <input type="tel" name="numero_telefono" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Correo electrónico</label>
                    <input type="email" name="correo" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Número de DPI</label>
                    <input type="number" name="Numero_Id" class="form-control" required>
                </div>
                
                <h3>Vinculación</h3>
                <div class="mb-3">
                    <label class="form-label">Número de cuenta bancaria</label>
                    <input type="number" name="cuenta_banco" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Tipo de cuenta</label>
                    <select name="tipo_Cuenta" class="form-select" required>
                        <option value="ahorro">Ahorro</option>
                        <option value="monetario">Monetario</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label">Dirección</label>
                    <input type="text" name="direccion" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Salario</label>
                    <input type="number" name="salario" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Fecha de contratación</label>
                    <input type="date" name="fecha_contratacion" class="form-control" required>
                </div>
                <button type="submit" class="btn btn-success">Registrar Empleado</button>
            </form>
        </div>
    </div>

    <div class="table-container">
        <h3 class="text-center">Lista de Empleados</h3>
        <div class="table-responsive">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Nombre</th>
                        <th>Fecha de Nacimiento</th>
                        <th>Teléfono</th>
                        <th>Correo</th>
                        <th>DPI</th>
                        <th>Cuenta Bancaria</th>
                        <th>Tipo Cuenta</th>
                        <th>Dirección</th>
                        <th>Salario</th>
                        <th>Fecha Contratación</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (data.length > 0) { %>
                        <% data.forEach((empleado, index) => { %>
                            <tr>
                                <td><%= index + 1 %></td>
                                <td><%= empleado.nombre %></td>
                                <td><%= empleado.fecha_nacimiento %></td>
                                <td><%= empleado.numero_telefono %></td>
                                <td><%= empleado.correo %></td>
                                <td><%= empleado.Numero_Id %></td>
                                <td><%= empleado.cuenta_banco %></td>
                                <td><%= empleado.tipo_Cuenta %></td>
                                <td><%= empleado.direccion %></td>
                                <td><%= empleado.salario %></td>
                                <td><%= empleado.fecha_contratacion %></td>
                                <td>
                                    <a href="/recursosHumanos/delete/<%= empleado.id %>" class="btn btn-danger btn-sm">Eliminar</a>
                                    <a href="/recursosHumanos/update/<%= empleado.id %>" class="btn btn-primary btn-sm">Actualizar</a>
                                </td>
                            </tr>
                        <% }) %>
                    <% } else { %>
                        <tr><td colspan="12" class="text-center">No hay empleados registrados</td></tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
</html>
